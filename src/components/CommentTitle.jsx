import { Typography } from '@mui/material';

export default function CommentTitle() {
	return (
		<Typography
			variant="h4"
			component="h1"
			sx={{ mb: 1, fontWeight: 'bold' }}
		>
			💬 Leave a Comment
		</Typography>
	);
}
